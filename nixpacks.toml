[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "pybabel compile -d app/translations"
]

[phases.deploy]
cmds = [
  "flask db upgrade"
]

[start]
cmd = "gunicorn run:app --workers 2 --bind 0.0.0.0:$PORT"
